[project]
name = "aws-iam-policy-manager"
version = "0.1.0"
description = "Generate and synchronize AWS IAM policies from templates"
requires-python = ">=3.11"

[tool.black]
line-length = 88

[tool.isort]
profile = "black"